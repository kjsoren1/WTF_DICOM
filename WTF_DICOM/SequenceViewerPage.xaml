<Page x:Class="WTF_DICOM.SequenceViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WTF_DICOM"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=local:SequenceViewerViewModel, IsDesignTimeCreatable=False}"
      Style="{StaticResource MaterialDesignWindow}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SequenceViewerPage">

  <DataGrid  ItemsSource="{Binding SequenceEntries}"
             x:Name="SequenceEntryDataGrid"
             AutoGenerateColumns="False"
             VerticalScrollBarVisibility="Visible"
             HorizontalScrollBarVisibility="Visible"
             IsReadOnly="True"
             CanUserDeleteRows="False"
             CanUserAddRows="False"
             CanUserReorderColumns="False"
             CanUserResizeColumns="True"
             CanUserSortColumns="False"
             SelectionMode="Single" SelectionUnit="CellOrRowHeader"
             AlternationCount="2" AlternatingRowBackground="DarkViolet">

  </DataGrid>

  <DataGrid.CellStyle>
    <Style TargetType="{x:Type DataGridCell}">
      <EventSetter Event="GotFocus" Handler="CellClick"/>
      <EventSetter Event="ContextMenu.ContextMenuOpening" Handler="DataGridContextMenuOpeningHandler"/>
    </Style>
  </DataGrid.CellStyle>
</Page>
