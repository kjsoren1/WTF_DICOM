<syncfusion:ChromelessWindow x:Class="WTF_DICOM.SimpleDicomFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:WTF_DICOM.Models" 
        d:DataContext="{d:DesignInstance Type=models:DicomFileCommon}"
        mc:Ignorable="d"
        Title="SimpleDicomFilesWindow" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <!--Menu-->
      <RowDefinition Height="auto" />
      <!--Toolbar/Searchbar-->
      <RowDefinition Height="auto" />
      <!--Header-->
      <RowDefinition Height="auto" />
      <!--Content-->
      <RowDefinition Height="*" />
      <!--Margin-->
      <RowDefinition Height="1" />
    </Grid.RowDefinitions>

    <!--Content - List of tags and values-->
    <DataGrid Grid.Row="3"
           ItemsSource="{Binding ReferencedOrRelatedDicomFiles}"
           x:Name="SimpleDicomFilesDataGrid"
           AutoGenerateColumns="False"
           VerticalScrollBarVisibility="Visible"
           HorizontalScrollBarVisibility="Visible"
           IsReadOnly="True"
           SelectionMode="Single" SelectionUnit="Cell"
           AlternationCount="2" AlternatingRowBackground="LightBlue">
      <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding Modality}" Header="Modality" />
        <DataGridTextColumn Binding="{Binding SOPInstanceUID}" Header="SOPInstanceUID" />
      </DataGrid.Columns>

    </DataGrid>

  </Grid>
</syncfusion:ChromelessWindow>
