<Window x:Class="WTF_DICOM.ReferencedSOPInstanceUIDsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WTF_DICOM"
        xmlns:models="clr-namespace:WTF_DICOM.Models" d:DataContext="{d:DesignInstance Type=models:ReferencedSOPInstanceUIDInfo}"
        mc:Ignorable="d"
        Title="ReferencedSOPInstanceUIDsWindow" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <!--Menu-->
      <RowDefinition Height="auto" />
      <!--Toolbar/Searchbar-->
      <RowDefinition Height="auto" />
      <!--Header-->
      <RowDefinition Height="auto" />
      <!--Content-->
      <RowDefinition Height="*" />
      <!--Margin-->
      <RowDefinition Height="1" />
    </Grid.RowDefinitions>

    <!--Content - List of tags and values-->
    <DataGrid Grid.Row="3"
         ItemsSource="{Binding ReferencedSOPInstanceUIDs}"
         x:Name="ReferencedSOPInstanceUIDsDataGrid"
         AutoGenerateColumns="False"
         VerticalScrollBarVisibility="Visible"
         HorizontalScrollBarVisibility="Visible"
         IsReadOnly="True"
         SelectionMode="Single" SelectionUnit="FullRow"
         AlternationCount="2" AlternatingRowBackground="DarkViolet">
      <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding DicomFileModality}" Header="Modality" />
        <DataGridTextColumn Binding="{Binding ReferencedSOPInstanceUID}" Header="ReferencedSOPInstanceUID" />
        <DataGridTextColumn Binding="{Binding DicomFileName}" Header="Dicom File Name" />
      </DataGrid.Columns>


      <DataGrid.RowStyle>
        <Style TargetType="{x:Type DataGridRow}"
               BasedOn="{StaticResource {x:Type DataGridRow}}">
          <EventSetter Event="ContextMenu.ContextMenuOpening" Handler="DataGridContextMenuOpeningHandler"/>
          <!--<Setter Property="Margin" Value="10,0,0,0"/>
          <Setter Property="VerticalAlignment" Value="Center"/>-->
        </Style>
      </DataGrid.RowStyle>

    </DataGrid>

  </Grid>
</Window>
